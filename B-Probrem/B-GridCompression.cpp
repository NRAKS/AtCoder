#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    int H, W;
    cin >> H >> W;
    vector<vector<char>> result(H, vector<char>(W));

    for (int i=0; i<H; i++){
        for (int j=0; j<W; j++){
            cin >> result.at(i).at(j);
        }
    }
    for (int h=0; h<H; h++){
        for (int w=0; w<W; w++){
            
        }
    }
    for (int w=0; w<W; w++){
        for (int h=0; h<H; h++){

        }
    }

}